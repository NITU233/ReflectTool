<?xml version='1.0' encoding='utf-8'?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nitu.reflect">

    <!-- 修改或删除您的SD卡中的内容 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 读取您的SD卡中的内容 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name="com.nitu.app.NITUApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppThemeDark"
        android:resizeableActivity="true"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name="com.nitu.reflect.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.nitu.reflect.SetValueActivity"/>
        <activity android:name="com.nitu.reflect.PreviewActivity"/>
        <activity android:name="com.nitu.reflect.RunActivity"/>
        <activity android:name="com.nitu.reflect.HelpActivity"/>
        <meta-data
            android:name="android.max_aspect"
            android:value="4.0"/>
    </application>
</manifest>
